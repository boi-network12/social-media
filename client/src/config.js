// config.js

const SERVER_URI = process.env.REACT_APP_SERVER_URI || "https://facebook-servside.onrender.com/api";

const config = {
    SERVER_URI,
};

export default config;



// // config.js

// const SERVER_URI = process.env.REACT_APP_SERVER_URI || "/api";

// const config = {
//     SERVER_URI,
// };

// export default config;
